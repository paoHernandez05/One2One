import { SaudiRiyal } from "lucide-react";
import styles from "./FunctioningTitle.module.css";

function FunctioningTitle({ title, subtitle }) {
  return <div className={styles.title}>{title}</div>;
}
